---
name: route
description: Scaffold a new App Router route with PageShell, metadata, JSON-LD, and a loading boundary. Use when adding a route or page under src/app/.
disable-model-invocation: true
---

# New route

Read and execute `.claude/commands/route.md` as the full procedure.

The user's slash arguments are the route name and optional `list` or `detail` — pass them as `$1` and `$2` where the command references them.

Before writing the page, read the `design-system` skill if you have not already.
