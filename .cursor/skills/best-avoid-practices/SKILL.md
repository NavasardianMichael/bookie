---
name: best-avoid-practices
description: Turn a practice to adopt or avoid into a durable, enforced rule in the right doc. Use when the user wants to codify a convention or anti-pattern.
disable-model-invocation: true
---

# Adopt or forbid a practice

Read and execute `.claude/commands/best-avoid-practices.md` as the full procedure.

The user's slash argument is the practice to evaluate — pass it as `$ARGUMENTS` where the command references it.
